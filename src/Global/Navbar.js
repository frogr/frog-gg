import React from "react";
import { Navbar, Nav, NavItem } from "react-bootstrap";

export const NavBar = () => {
  return (
    <Navbar inverse>
      <Nav>
        <NavItem> frogs </NavItem>
      </Nav>
    </Navbar>
  );
};
